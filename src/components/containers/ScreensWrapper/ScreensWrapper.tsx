import React from 'react';
import { StyledView } from './styles';

const ScreensWrapper: React.FC = ({ children }) => {
  return <StyledView>{children}</StyledView>;
};

export default ScreensWrapper;
